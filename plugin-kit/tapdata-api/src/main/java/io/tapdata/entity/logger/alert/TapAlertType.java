package io.tapdata.entity.logger.alert;

/**
 * Stable routing type for structured task alerts.
 * Notification policy stays in TM; connectors and the engine only report this type.
 */
public enum TapAlertType {
    DATA_INTEGRITY
}
